{
  "name": "cursor_token_usage",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --sourcemap --format=iife --target=es2022 --outfile=public/main.js",
    "dev": "esbuild src/main.ts --bundle --sourcemap --format=iife --target=es2022 --outfile=public/main.js --watch",
    "start": "npm run dev",
    "release": "npm run build && node scripts/prepare-release.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  }
}
