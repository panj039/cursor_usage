---
alwaysApply: true
---

- 不要生成测试文件, 如果必须要生成, 则放在 `tmp/` 文件夹中
- 不需要生成说明文件
- 不要保持向后兼容的内容, 统一修改全部相关内容
- 不要过度设计, 没有用到的东西一律不生成
- 不论是同步还是异步, 都只需要一个有效的可用接口
- 代码尽量简练, 简单清晰
- 一个功能只要一个实现即可, 尽量复用现有功能
- 不需要提供任何`Mock`代码
